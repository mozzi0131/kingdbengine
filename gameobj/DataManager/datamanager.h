// save, load, log the data into the game's data folder
// It will be great to encrypt the data, and decrypt  only when we need
// (if we can...)

#ifndef DATAMANAGER_DATAMANAGER_H
#define DATAMANAGER_DATAMANAGER_H

class DataManager
{
  DataManager();
  ~DataManager();

public:
  
private:
};

#endif // DATAMANAGER_DATAMANAGER_H